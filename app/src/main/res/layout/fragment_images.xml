<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="?attr/backgroundApp"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".fragments.photos.ImagesFragment">
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tabLayout"
        android:background="?attr/tabLayoutColor"
        app:layout_constraintTop_toTopOf="parent"
        app:tabMode="scrollable"
        app:tabIndicatorColor="?attr/tabIndicatorColor1"
        app:tabTextColor="?attr/tabIndicatorColor1"
        app:tabTextAppearance="@style/TabStyle"
        app:tabIndicatorHeight="@dimen/_2sdp"
        app:tabGravity="center"
        app:tabIndicatorGravity="bottom"
        app:tabRippleColor="@null"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"/>
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewpager2"
        app:layout_constraintTop_toBottomOf="@id/tabLayout"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_width="match_parent"
        android:layout_height="0dp"/>
</androidx.constraintlayout.widget.ConstraintLayout>